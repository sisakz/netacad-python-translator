<problem chapter='2' section='4' slide='1' problem='10' title='LAB: Operators and expressions'>

<!--
Update history:
2020-07-17 Grzegorz (reviewed, code - blank lines added to the bottom of the snippet - editor, hint, sample solution)
-->

<code>x =  # hardcode your test data here
x = float(x)
# write your code here
print("y =", y)

</code>

<span class="label" style="background-color:rgb(205, 168, 105);"><strong>LAB</strong></span>

<br><br>

<h2>Estimated time</h2>
<p>10-15 minutes</p>

<h2>Level of difficulty</h2>
<p>Easy</p>

<h2>Objectives</h2>
<ul>
<li>becoming familiar with the concept of numbers, operators, and arithmetic operations in Python;</li>
<li>performing basic calculations.</li>
</ul>


<h2>Scenario</h2>

<p>Take a look at the code in the editor: it reads a <codel>float</codel> value, puts it into a variable named <codel>x</codel>, and prints the value of a variable named <codel>y</codel>. Your task is to complete the code in order to evaluate the following expression:</p>

<p><font size="+1"> 3x<sup>3</sup> - 2x<sup>2</sup> + 3x - 1 </font></p>


<p>The result should be assigned to <codel>y</codel>.</p>

<p>Remember that classical algebraic notation likes to omit the multiplication operator ‒ you need to use it explicitly. Note how we change data type to make sure that <codel>x</codel> is of type <codel>float</codel>.</p>

<p>Keep your code clean and readable, and test it using the data we've provided, each time assigning it to the <codel>x</codel> variable (by hardcoding it). Don't be discouraged by any initial failures. Be persistent and inquisitive.</p>

<br>

<h2>Test Data</h2>


<p>Sample input</p>

<codep>x = 0
x = 1
x = -1</codep>

<br>

<p>Expected Output</p>

<codep>y = -1.0
y = 3.0
y = -9.0</codep>
<p style="text-align: right;"><font size="-2"><b>output</b></font></p>

<br>

<labMode>true</labMode>

<hint>x = 0
x = float(x)
y =  # write the notion here
print("y =", y)

x = 1
x = float(x)
y =  # write the notion here
print("y =", y)

# write the rest of your code here

</hint>


<sampleSolution>x = 0
x = float(x)
y = 3 * x**3 - 2 * x**2 + 3 * x - 1
print("y =", y)

x = 1
x = float(x)
y = 3 * x**3 - 2 * x**2 + 3 * x - 1
print("y =", y)

x = -1
x = float(x)
y = 3 * x**3 - 2 * x**2 + 3 * x - 1
print("y =", y)

</sampleSolution>

</problem>